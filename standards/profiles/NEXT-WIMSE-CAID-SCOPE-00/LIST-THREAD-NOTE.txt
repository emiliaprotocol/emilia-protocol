Rafael,

Thank you for rebuilding the evaluator and checking the refusal reasons. Your four points were right.

For this profile, tool.call.1 now requires a nonempty occurrence_id. A transport retry of the same logical invocation presents the same complete action, occurrence ID, and CAID. A second intended invocation gets a new occurrence ID and CAID. Reusing one occurrence with different material remains a stateful admission conflict; this mapping does not claim exactly-once execution.

You were also right about the packet. I omitted validate.mjs from the emailed set. The complete validator is now in the public packet, and the reproducible unit is the PR checkout because it imports the pinned CAID implementation and registry.

I added direct refusals for an unknown detail member, non-array constraints, pay.* against payment.release, the invalid bare prefix, a same-invocation retry, a new occurrence with the old CAID, and changed material under the original occurrence and CAID. The revised 22-vector set passes locally.

The explicit mappings, the agent_delegation pin, and the claim boundary are unchanged.

Best,
Iman
