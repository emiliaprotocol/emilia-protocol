# Authority Introduction -03 upload packet

This directory is the isolated upload packet for
`draft-schrock-ep-authority-introduction-03`. It is deliberately separate from
the immutable August 3 six-draft provenance packet in `../UPLOAD-THIS/`.

Upload only:

- `UPLOAD-THIS/draft-schrock-ep-authority-introduction-03.xml`

Revision -03 adds the source-resolution boundary for authority evidence. A
new signature cannot make an older directory, entitlement, delegation, or
revocation observation fresher or more precise than its source. The revision
also records the reference signer's fail-closed behavior for missing and
unknown revocation observations and adds informative composition context for
OMP and AGTP-LEI.

The document remains an individual Informational Internet-Draft. Submission
does not make it an RFC, a working-group document, IETF consensus, or IETF
endorsement.
