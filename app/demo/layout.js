export const metadata = {
  title: 'The Agent That Tried To — Live Crash Test | EMILIA Protocol',
  description:
    'Watch an autonomous AI agent get prompt-injected into a catastrophic action — a $2.4M wire, a dropped production database, 312 redirected benefits — and watch EMILIA block it at the pre-execution moment with a signed refusal. Enforced by the real policy engine.',
  alternates: { canonical: '/demo' },
  openGraph: {
    title: 'The Agent That Tried To — Live Crash Test',
    description:
      'An AI agent is manipulated into a catastrophic action. EMILIA stops it before it executes, with a cryptographic receipt. Nothing irreversible without a signed human yes.',
    url: 'https://www.emiliaprotocol.ai/demo',
    type: 'website',
  },
};

export default function DemoLayout({ children }) {
  return children;
}
