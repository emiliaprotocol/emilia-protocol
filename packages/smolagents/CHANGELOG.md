# Changelog

## 0.1.1 (2026-09-05)

- Require `emilia-crewai>=0.3.5,<0.4` and `emilia-verify>=2.8.5,<3` for the
  corrected Python release chain. Adapter behavior is unchanged from 0.1.0.
- Use a fresh release version; the existing 0.1.0 source tag stays immutable.

## 0.1.0 (2026-09-05)

- Add the exact-action receipt wrapper for synchronous smolagents tools,
  including a final freshness check after tool setup and a synthetic refund demo.
- Source tagged, but not uploaded to PyPI before the dependency packaging fix.
