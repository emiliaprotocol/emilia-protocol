{
  "@version": "EP-SECURITY-CASE-VECTORS-v1",
  "suite": "Executable vectors for claims whose acceptance path is stateful or release-operational",
  "vectors": [
    {
      "id": "accept_device_signed_denial",
      "expect": { "valid": true },
      "execution": { "runner": "vitest", "file": "tests/signoff-decision-evidence.test.js", "title": "exports a device-signed denial that verifies offline under a pinned approver key" }
    },
    {
      "id": "reject_signed_denial_relabel",
      "expect": { "valid": false },
      "execution": { "runner": "vitest", "file": "tests/webauthn-approve-route.test.js", "title": "refuses to relabel a device-signed denial as approval" }
    },
    {
      "id": "reject_class_a_bearer_denial",
      "expect": { "valid": false },
      "execution": { "runner": "vitest", "file": "tests/v1-api.test.js", "title": "returns 403 when bearer-key rejection attempts a Class-A-required receipt" }
    },
    {
      "id": "accept_monotonic_witness_sequence",
      "expect": { "valid": true },
      "execution": { "runner": "node-test", "file": "packages/gate/network-witness.test.js", "title": "accepts a higher witness sequence before any conflict" }
    },
    {
      "id": "reject_witness_stream_after_equivocation",
      "expect": { "valid": false },
      "execution": { "runner": "node-test", "file": "packages/gate/network-witness.test.js", "title": "permanently refuses every later sequence after same-sequence equivocation" }
    },
    {
      "id": "accept_single_reservation_winner",
      "expect": { "valid": true },
      "execution": { "runner": "node-test", "file": "packages/gate/store-faults.test.js", "title": "fault: a 100-way reservation storm admits exactly one worker" }
    },
    {
      "id": "reject_stale_reservation_owner",
      "expect": { "valid": false },
      "execution": { "runner": "node-test", "file": "packages/gate/store-faults.test.js", "title": "fault: stale owner cannot erase or commit a replacement owner" }
    },
    {
      "id": "accept_5000_linearizable_schedules",
      "expect": { "valid": true },
      "execution": { "runner": "node-test", "file": "packages/gate/store-linearizability.test.js", "title": "generated model: 5000 schedules preserve linearizable at-most-once effects" }
    },
    {
      "id": "reject_stale_replica_promotion",
      "expect": { "valid": false },
      "execution": { "runner": "node-test", "file": "packages/gate/store-linearizability.test.js", "title": "stale replica promotion and rollback attempts fail closed" }
    },
    {
      "id": "accept_durable_challenge_after_restart",
      "expect": { "valid": true },
      "execution": { "runner": "vitest", "file": "tests/evidence-challenge-durable.test.js", "title": "survives restart and admits exactly one concurrent presentation" }
    },
    {
      "id": "reject_tampered_registered_challenge",
      "expect": { "valid": false },
      "execution": { "runner": "vitest", "file": "tests/evidence-challenge-durable.test.js", "title": "binds registration to the entire challenge body, not only id and nonce" }
    },
    {
      "id": "reject_challenge_policy_drift",
      "expect": { "valid": false },
      "execution": { "runner": "vitest", "file": "tests/evidence-challenge-durable.test.js", "title": "refuses policy drift without consuming the registered challenge" }
    },
    {
      "id": "accept_successful_effect_once",
      "expect": { "valid": true },
      "execution": { "runner": "node-test", "file": "packages/gate/gate.test.js", "title": "run() commits a successful effect and permanently refuses replay" }
    },
    {
      "id": "reject_ambiguous_effect_retry",
      "expect": { "valid": false },
      "execution": { "runner": "node-test", "file": "packages/gate/gate.test.js", "title": "run() consumes an indeterminate attempt when the executor throws" }
    },
    {
      "id": "accept_atomic_aec_evidence_across_replicas",
      "expect": { "valid": true },
      "execution": { "runner": "node-test", "file": "packages/gate/evidence-atomic.test.js", "title": "atomic evidence: concurrent replicas append one linear chain" }
    },
    {
      "id": "reject_aec_replay_via_decoy_component",
      "expect": { "valid": false },
      "execution": { "runner": "vitest", "file": "tests/aec-execution-gate.test.js", "title": "cannot mint a fresh replay key with an invalid decoy component" }
    },
    {
      "id": "accept_phi_minimized_projection",
      "expect": { "valid": true },
      "execution": { "runner": "vitest", "file": "tests/ncpdp-privacy.test.js", "title": "projects an appeal bundle without recursively copying planted PHI" }
    },
    {
      "id": "reject_direct_patient_data",
      "expect": { "valid": false },
      "execution": { "runner": "vitest", "file": "tests/ncpdp-privacy.test.js", "title": "refuses direct identifiers, bare hashes, free text, and instance-specific appeal URLs" }
    },
    {
      "id": "accept_purpose_bound_disclosure",
      "expect": { "valid": true },
      "execution": { "runner": "vitest", "file": "tests/ncpdp-privacy.test.js", "title": "discloses only to the pinned audience and purpose within the retention budget" }
    },
    {
      "id": "reject_health_disclosure_substitution",
      "expect": { "valid": false },
      "execution": { "runner": "vitest", "file": "tests/ncpdp-privacy.test.js", "title": "refuses purpose, audience, retention, artifact, and field-smuggling attacks" }
    },
    {
      "id": "accept_byte_identical_registry_artifact",
      "expect": { "valid": true },
      "execution": { "runner": "vitest", "file": "tests/release-reproducibility.test.js", "title": "accepts a registry artifact only when every published byte matches" }
    },
    {
      "id": "reject_registry_byte_substitution",
      "expect": { "valid": false },
      "execution": { "runner": "vitest", "file": "tests/release-reproducibility.test.js", "title": "rejects a one-byte registry artifact substitution" }
    },
    {
      "id": "accept_byte_identical_pypi_wheel",
      "expect": { "valid": true },
      "execution": { "runner": "vitest", "file": "tests/release-reproducibility.test.js", "title": "accepts a PyPI wheel only when every published byte matches" }
    },
    {
      "id": "reject_pypi_wheel_substitution",
      "expect": { "valid": false },
      "execution": { "runner": "vitest", "file": "tests/release-reproducibility.test.js", "title": "rejects a one-byte PyPI wheel substitution" }
    },
    {
      "id": "accept_byte_identical_pypi_sdist",
      "expect": { "valid": true },
      "execution": { "runner": "vitest", "file": "tests/release-reproducibility.test.js", "title": "accepts a PyPI sdist only when every published byte matches" }
    },
    {
      "id": "reject_pypi_sdist_substitution",
      "expect": { "valid": false },
      "execution": { "runner": "vitest", "file": "tests/release-reproducibility.test.js", "title": "rejects a one-byte PyPI sdist substitution" }
    },
    {
      "id": "accept_all_declared_release_chains",
      "expect": { "valid": true },
      "execution": { "runner": "vitest", "file": "tests/release-chain-coverage.test.js", "title": "every declared package uses its complete verifiable release chain" }
    },
    {
      "id": "accept_npm_pypi_release_byte_chains",
      "expect": { "valid": true },
      "execution": { "runner": "vitest", "file": "tests/release-chain-coverage.test.js", "title": "every declared npm and PyPI package uses reproducible registry-byte verification" }
    },
    {
      "id": "accept_go_tag_proxy_chain",
      "expect": { "valid": true },
      "execution": { "runner": "vitest", "file": "tests/release-chain-coverage.test.js", "title": "Go release isolates tag write authority and verifies the public proxy origin" }
    },
    {
      "id": "reject_go_release_without_proxy_comparison",
      "expect": { "valid": false },
      "execution": { "runner": "vitest", "file": "tests/release-chain-coverage.test.js", "title": "refuses a Go tag publisher without public-proxy source comparison" }
    },
    {
      "id": "reject_go_release_write_job_code_execution",
      "expect": { "valid": false },
      "execution": { "runner": "vitest", "file": "tests/release-chain-coverage.test.js", "title": "refuses Go release code execution in the contents-write tag job" }
    },
    {
      "id": "reject_go_release_mutable_or_credentialed_checkout",
      "expect": { "valid": false },
      "execution": { "runner": "vitest", "file": "tests/release-chain-coverage.test.js", "title": "refuses a Go release checkout that follows mutable main or keeps credentials" }
    },
    {
      "id": "reject_unclassified_go_module",
      "expect": { "valid": false },
      "execution": { "runner": "vitest", "file": "tests/release-chain-coverage.test.js", "title": "refuses an unclassified Go module even when its release manifest is absent" }
    },
    {
      "id": "reject_release_chain_without_registry_comparison",
      "expect": { "valid": false },
      "execution": { "runner": "vitest", "file": "tests/release-chain-coverage.test.js", "title": "refuses a reusable publisher with its post-registry byte comparison removed" }
    },
    {
      "id": "accept_owner_approved_release_dispatch",
      "expect": { "valid": true },
      "execution": { "runner": "vitest", "file": "tests/release-approval.test.js", "title": "accepts an owner dispatch bound to the exact package version and tag" }
    },
    {
      "id": "reject_unapproved_release_dispatch",
      "expect": { "valid": false },
      "execution": { "runner": "vitest", "file": "tests/release-approval.test.js", "title": "rejects automatic events, other actors, tag drift, and weak confirmation" }
    },
    {
      "id": "accept_model_to_matter_once",
      "expect": { "valid": true },
      "execution": { "runner": "vitest", "file": "tests/model-to-matter.test.js", "title": "admits at most one clearance across distinct challenges for the same action" }
    },
    {
      "id": "reject_model_to_matter_without_revocation_state",
      "expect": { "valid": false },
      "execution": { "runner": "vitest", "file": "tests/model-to-matter.test.js", "title": "fails before challenge consumption when explicit revocation state is absent" }
    },
    {
      "id": "accept_action_escrow_exact_release_once",
      "expect": { "valid": true },
      "execution": { "runner": "node-test", "file": "packages/gate/action-escrow.test.js", "title": "runs the complete milestone lifecycle and releases exactly once" }
    },
    {
      "id": "reject_action_escrow_committed_fact_substitution",
      "expect": { "valid": false },
      "execution": { "runner": "node-test", "file": "packages/gate/action-escrow.test.js", "title": "signed action commitments cannot be replaced by verifier-selected funding or evidence" }
    },
    {
      "id": "reject_action_escrow_role_substitution",
      "expect": { "valid": false },
      "execution": { "runner": "node-test", "file": "packages/gate/action-escrow-evidence.test.js", "title": "document execution, agreement acceptance, and release approval are non-substitutable rows" }
    },
    {
      "id": "reject_action_escrow_funded_amendment_without_unwind",
      "expect": { "valid": false },
      "execution": { "runner": "node-test", "file": "packages/gate/action-escrow.test.js", "title": "an amendment cannot race funded custody without an authenticated unwind" }
    },
    {
      "id": "reject_action_escrow_journal_divergence",
      "expect": { "valid": false },
      "execution": { "runner": "node-test", "file": "packages/gate/action-escrow-postgres.test.js", "title": "normal reads refuse truncated, reordered, or state-divergent journals" }
    },
    {
      "id": "reject_action_escrow_verifier_echo_substitution",
      "expect": { "valid": false },
      "execution": { "runner": "node-test", "file": "packages/gate/action-escrow-evidence.test.js", "title": "component verifiers must echo the exact agreement, binding, and action they checked" }
    },
    {
      "id": "reject_action_escrow_expired_release_approval",
      "expect": { "valid": false },
      "execution": { "runner": "node-test", "file": "packages/gate/action-escrow.test.js", "title": "expired approvals are refused both at admission and immediately before release" }
    },
    {
      "id": "reject_action_escrow_funding_request_state_fallback",
      "expect": { "valid": false },
      "execution": { "runner": "node-test", "file": "packages/gate/action-escrow.test.js", "title": "funding-request state blocks amendment and cancellation until custodian reconciliation" }
    },
    {
      "id": "reject_action_escrow_compensated_history_pruning",
      "expect": { "valid": false },
      "execution": { "runner": "node-test", "file": "packages/gate/action-escrow-package.test.js", "title": "assembler refuses pruned operation history and regressing timestamps" }
    },
    {
      "id": "reject_action_escrow_database_role_or_clock_rollback",
      "expect": { "valid": false },
      "execution": { "runner": "node-test", "file": "packages/gate/action-escrow-postgres.test.js", "title": "health refuses owner or destructive runtime privileges" }
    },
    {
      "id": "reject_action_escrow_custodian_alias_or_unfunded_release",
      "expect": { "valid": false },
      "execution": { "runner": "vitest", "file": "tests/action-escrow-escrow-com.test.js", "title": "refuses milestone acceptance until every schedule is secured and funded" }
    },
    {
      "id": "accept_action_escrow_project_source_snapshot",
      "expect": { "valid": true },
      "execution": { "runner": "node-test", "file": "examples/action-escrow/scenario.test.mjs", "title": "keeps document, exact-action approvals, and external custody visibly separate" }
    },
    {
      "id": "reject_action_escrow_project_source_substitution",
      "expect": { "valid": false },
      "execution": { "runner": "node-test", "file": "packages/gate/action-escrow-verifiers.test.js", "title": "the contractor profile requires a project source digest bound to its signed term" }
    },
    {
      "id": "reject_action_escrow_project_sidecar_bytes_substitution",
      "expect": { "valid": false },
      "execution": { "runner": "node-test", "file": "examples/action-escrow/scenario.test.mjs", "title": "uses the shipped DAB, Action Escrow kernel, and portable evidence package" }
    },
    {
      "id": "reject_action_escrow_project_source_context_relabeling",
      "expect": { "valid": false },
      "execution": { "runner": "node-test", "file": "examples/action-escrow/scenario.test.mjs", "title": "refuses project-source context relabeling even after package rehash" }
    }
  ]
}
