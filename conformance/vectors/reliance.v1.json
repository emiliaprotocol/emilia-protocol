{
  "suite": "EP-RELIANCE-KERNEL-v1",
  "profile": "reliance-verdict",
  "vectors_version": "2026-07-07",
  "note": "Reliance verdicts over a composed evidence packet against a pinned EP-RELIANCE-PROFILE-v1. Scenario-driven: because a valid trust receipt + authority proof require live signatures, each vector names a `break` the test applies to a fully-valid base packet, then asserts the closed verdict. expect.valid mirrors verdict==='rely' so the admissibility checker recognizes the refusal cases. Positive + a reject for every do_not_rely_* verdict.",
  "vectors": [
    { "id": "rely_full_packet_composes", "break": "none", "expect": { "valid": true, "verdict": "rely" } },
    { "id": "reject_unsigned_receipt", "break": "tamper_signoff", "expect": { "valid": false, "verdict": "do_not_rely_unsigned" } },
    { "id": "reject_untrusted_issuer", "break": "unpinned_issuer", "expect": { "valid": false, "verdict": "do_not_rely_untrusted_issuer" } },
    { "id": "reject_no_class_a", "break": "class_b_only", "expect": { "valid": false, "verdict": "do_not_rely_no_class_a" } },
    { "id": "reject_quorum_unsatisfied", "break": "require_quorum_absent", "expect": { "valid": false, "verdict": "do_not_rely_quorum_unsatisfied" } },
    { "id": "reject_authority_missing", "break": "no_authority_proof", "expect": { "valid": false, "verdict": "do_not_rely_authority_missing" } },
    { "id": "reject_authority_revoked", "break": "authority_revoked", "expect": { "valid": false, "verdict": "do_not_rely_authority_revoked" } },
    { "id": "reject_authority_expired", "break": "authority_expired", "expect": { "valid": false, "verdict": "do_not_rely_authority_expired" } },
    { "id": "reject_scope_mismatch", "break": "authority_scope_wrong", "expect": { "valid": false, "verdict": "do_not_rely_scope_mismatch" } },
    { "id": "reject_amount_exceeded", "break": "amount_over_ceiling", "expect": { "valid": false, "verdict": "do_not_rely_amount_exceeded" } },
    { "id": "reject_policy_mismatch", "break": "policy_not_accepted", "expect": { "valid": false, "verdict": "do_not_rely_policy_mismatch" } },
    { "id": "reject_stale_revocation", "break": "revocation_stale", "expect": { "valid": false, "verdict": "do_not_rely_stale_revocation" } },
    { "id": "reject_already_consumed", "break": "already_consumed", "expect": { "valid": false, "verdict": "do_not_rely_already_consumed" } },
    { "id": "reject_registry_unavailable", "break": "registry_key_unpinned", "expect": { "valid": false, "verdict": "do_not_rely_registry_unavailable" } }
  ]
}
