# Android reference app

The sample app covers device enrollment, exact-action review, signed approval or
denial, Credential Manager passkeys, Play Integrity, and server consumption.

Before running it, replace the example application ID and API URL and set
`PLAY_CLOUD_PROJECT_NUMBER` to the deploying agency's Google Cloud project.
The example identifiers do not claim a customer or government endorsement.
