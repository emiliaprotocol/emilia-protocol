# IETF Submission Runbook — 2026-06-28 architecture cluster

**Goal:** post the EMILIA authorization architecture as one coherent cluster ahead of the
2026-06-30 Joint Dispatch/SecDispatch interim. All drafts are dated 2026-06-28 and render via
xml2rfc. idnits: clean except the expected UTF-8 typographic note (em-dashes), which modern
I-Ds permit — not an error.

**Pre-submission fixes applied 2026-06-26 (post audit):** architecture §3.6 corrected — EMILIA
Eye is described as *authorization-posture advisories* (never the gate), not as the
"identity-binding" component (that role is now stated honestly as device-bound user verification
+ an identity-binding profile, work in progress); EMILIA Eye relabeled **Experimental** (matches
its body); architecture adds a DRP cede-and-distinguish bullet + AIPF/discovery cedes; evidence-
chain date corrected to 28 June. Lead the positioning on the **human-authorization apex**.

## Submit (you must do this — needs your IETF datatracker login + email confirmation)

Upload each `.xml` (preferred — datatracker renders the .txt) at
**https://datatracker.ietf.org/submit/**, then confirm via the email datatracker sends to
**team@emiliaprotocol.ai**. Files are in `standards/`.

**Order (anchor first, so the overview is up when the components land):**

1. `draft-schrock-ep-architecture-00.xml` — the umbrella overview (NEW)
2. `draft-schrock-ep-authorization-receipts-03.xml` — revision of the posted -02 (adds agent_binding L4→L7 + apex framing)
3. `draft-schrock-ep-authorization-evidence-chain-00.xml` — the composition seam (NEW) ⭐
4. `draft-schrock-ep-evidence-record-00.xml` — long-term crypto-agile preservation (NEW)
5. `draft-schrock-ep-enforcement-point-00.xml` — the Receipt-Required rail (NEW)
6. `draft-schrock-emilia-eye-00.xml` — authorization-posture advisories (NEW, **Experimental**)

**Already posted — do NOT re-submit:** `draft-schrock-ep-quorum-01` (live).

**Before uploading each:** confirm on datatracker that the -00 isn't already posted under
your name (our records say these five are unfiled, but verify; if any is already up, skip it).

## After all six are posted + confirmed

Send the **single** list announcement (staged as a Gmail draft to secdispatch@ietf.org —
do NOT send six separate emails; one coherent note is what keeps this a "category," not a
flood). Recommended timing: once the datatracker links resolve, and before/at the interim.

## Held for fast-follow (NOT in this batch — deliberate)

- `draft-schrock-ep-human-oversight-profile-00` (HITL/HOTL, autonomy/MHC). The flag is
  already planted by the umbrella §3.7 and PIP-013. File this a few days later / after the
  interim, framed civilian-first (EU AI Act Art. 14 + NIST lead; DoD 3000.09 as one further
  applicability) — leading the architecture's debut with defense framing is the one thing
  that could draw an objection that taints the cluster.

## Discipline gates (why this reads as a body of work, not spam)

- One umbrella that names every component → it's an architecture, not a pile.
- Each component a distinct, valid, idnits-clean claim.
- One announcement, humble, framed as composing with (not competing with) identity /
  delegation / policy / transparency.
