# OpenAI Cybersecurity Grant — REVISION (paste-ready) — 2026-06-27

**Revision of the 2026-06-13 submission.** The program explicitly invites a marked full-resubmission ("if you refine your idea later, submit the updated proposal in full and mention it's a revision"). Submit the FULL form again from team@emiliaprotocol.ai; keep the $250k ask (credits primary) + the part-time-cryptographer line.

> NOTE before submitting: this copy references `@emilia-protocol/openai-agents` as a published npm package and two open external PRs. Make sure the npm publish is done first (see publish prep), and the PR links are live. Both are true as of 2026-06-27.

---

### Project title
(unchanged) EMILIA Protocol: an open, formally-verified standard that gates every irreversible AI-agent action behind a cryptographic authorization ceremony and emits a portable receipt any defender can verify offline.

### What problem are you trying to solve? (<=200 words)
(unchanged — still accurate)
AI agents are shifting from recommendation to autonomous action — deploying code, moving money, rotating credentials, calling tools with real consequences. Cyber defense's question shifts from "is the model accurate?" to "did this agent have authorization to take this specific irreversible action on behalf of this principal — and can a defender prove it afterward?"

Existing primitives don't answer that. OAuth and API keys prove "this caller has this scope," not "this caller authorized this exact action under this policy"; once a key is exfiltrated or a session hijacked, every downstream action is silently authorized. RBAC/ABAC has no native concept of action-bound consent or replay protection for agent-to-agent delegation. SIEM and audit logs record events after the fact, unstructured, and are produced by the same system whose integrity is in question. Prompt-injection defenses harden the model's reasoning but leave no artifact a third party can check — when an injected agent acts, nothing portable distinguishes an authorized action from a bypass.

There is no open standard for verifiable pre-action authorization in agent systems, and no portable evidence for responders to reconstruct who authorized what.

### Project proposal (plaintext, no links, <=3000 words)

**Revision of our 2026-06-13 submission. Since then we shipped the milestones this proposal projected — ahead of schedule.** The single IETF draft is now a complete, posted cluster; cross-language conformance is up to 8 suites / 58 vectors; the first external integrations that *require* an EMILIA receipt are open against third-party agent servers; and we shipped an adapter that plugs EP directly into OpenAI's own Agents SDK approval step. The proposal below reflects that.

EMILIA Protocol (EP) is an open standard and Apache-2.0 reference implementation for authorization receipts: cryptographic, offline-verifiable proof that a named principal authorized an exact irreversible AI-agent action, under a stated policy, before it executed. The receipt is the artifact. It verifies anywhere, with open-source code, independent of the system that issued it — defenders do not have to trust the operator's runtime, logs, or continued existence.

EP gates each irreversible action behind a short ceremony — risk watch, pre-action handshake, named human signoff for high-stakes actions, then an atomic sealed commit — and emits a receipt that cryptographically binds: (1) the canonical hash of the exact action, so a receipt presented for any other action fails; (2) a hash-pinned policy version, so mutating policy between authorization and execution fails verification, closing silent-upgrade attacks; (3) a server-issued nonce for replay resistance; (4) one-time consumption, formally proven, so an accepted authorization is consumed at most once; (5) authority-chain integrity, binding the actor to a specific delegation chain with proven acyclicity; (6) a named, accountable human signoff hash-bound to the exact action and policy; and (7) append-only, Merkle-anchored logging of every transition for independent audit.

The property most relevant to agentic defense is containment. Prompt injection can change what an agent proposes, but it cannot forge the device-bound human approval or the signed receipt. Either a valid receipt exists for a gated action, or it does not — and the absence of a receipt for a gated action is itself a positive bypass signal, not merely a missing log line.

**What changed since 6/13 — it ships, and it's now adopted at the edges.** The specification is no longer a single draft: the full architecture cluster is posted to the IETF datatracker — `draft-schrock-ep-authorization-receipts` (now at revision -03, adding an agent-binding member), plus `-quorum` (two-person / m-of-n human authorization), `-enforcement-point` (the fail-closed "no receipt, no execution" rail, including a post-execution receipt), `-authorization-evidence-chain` (composition of heterogeneous receipts under one action digest), `-evidence-record` (long-term, crypto-agile preservation for multi-year retention), `-architecture` (the umbrella), and an experimental posture-advisory draft. There are published, zero-dependency npm packages (`@emilia-protocol/verify`, `@emilia-protocol/issue` — "issue locally, verify anywhere") plus independent verifiers in JavaScript, Python, and Go and a public conformance suite now at **8 suites / 58 cross-language vectors that all three implementations agree on**. Safety is machine-checked: 26 TLA+ properties verified by TLC across 413,137 states with zero errors, and 22 Alloy assertions with zero counterexamples, re-run in CI. An 85-case red-team suite includes prompt-injection containment.

**Fit with this program — EP now sits directly on OpenAI's own approval primitive.** OpenAI's Agents SDK already pauses an agent and asks a human to approve a tool call (`needsApproval` / `result.interruptions` / `state.approve|reject`). That is useful at runtime, but after the fact an enterprise still cannot prove *who* approved, *what exact action* they saw, whether it was altered or replayed, or verify any of it without trusting OpenAI's own trace. We shipped `@emilia-protocol/openai-agents`, an open adapter that turns that approval step into a verifiable receipt: missing receipt → the tool stays blocked; a valid receipt bound to the exact tool call → it runs; a replayed receipt → refused; a tampered receipt → refused. EMILIA does not replace OpenAI's approval — it makes that approval portable, offline-verifiable evidence. The MCP guard remains the cross-platform path for any MCP-speaking agent. EP is purely defensive — it gates and proves agent actions and has no offensive capability.

**External adoption is already underway, ahead of our own timeline.** Our 6/13 timeline projected "first external party requiring receipts" for October. As of late June, the first external Receipt-Required integrations are open as pull requests into third-party agent/MCP servers — a Notion MCP server and a Baserow MCP server — each adding the same RR-1 pattern in front of an irreversible delete: missing receipt → HTTP 428, valid runs, replay and tamper refused, proven against the real handler. One PR is a request; a repeatable pattern landing in repo after repo is how a defensive convention forms.

**What we would build with this grant.** First, cross-language verifier and adapter hardening: extend the three verifiers, the conformance suite, and the OpenAI Agents adapter to fully cover the agent-binding and escalation-attestation paths, so any defender's stack validates agent approvals independently; OpenAI API credits fund codegen-assisted porting and test generation. Second, we will create and openly release the **EP Agentic Authorization-Containment Benchmark**: a labeled dataset of adversarial agent scenarios — prompt-injection variants paired with pass/fail receipt outcomes and policy-defined escalation ground truth — published under a permissive license (Apache-2.0 / CC-BY) as a reusable eval any lab can run against any agent model, evaluated against **GPT-5.5, GPT-5.4, and GPT-5.3 Instant** on OpenAI API credits, empirically measuring the containment claim (an injected proposal never yields a valid receipt for a gated action). Third, an independent cryptographic implementation audit (Trail of Bits / NCC / Cure53 class) of canonicalization, signing, and cross-verification. Fourth, an OpenAI-Agents-SDK + MCP guard reference deployment with defender onboarding docs, building on the external integrations already in flight. Grant funds a part-time contract cryptographer alongside the PI.

**Release plan:** the protocol, the three verifiers, the conformance suite, the OpenAI Agents adapter, and the new benchmark dataset + methodology + results are all published openly (Apache-2.0 / CC-BY); findings shared early per the program's collaboration norm. Purely defensive — no offensive-security component.

**Honest scope.** EP has no signed production customers yet; the external integrations above are open pull requests, not merged adoptions, and a government observe-mode pilot is in outreach, not signed. EP claims only what the cryptography delivers: a receipt proves a named principal authorized a specific action under a specific policy before execution — not that the decision was wise, lawful, or correct, and not the initiator's internal reasoning. That precision is deliberate.

**Team.** Iman Schrock, founder and principal investigator, authored the full stack: the IETF cluster, the TLA+ and Alloy models, the red-team suite, the three-language verifiers, the npm toolkit, and the OpenAI Agents adapter. All artifacts are Apache-2.0 and public.

### Project timeline
Shipped ahead of plan (by June 2026): full IETF cluster posted; 8 suites / 58 cross-language conformance vectors; `@emilia-protocol/openai-agents` adapter; first external Receipt-Required PRs into third-party MCP servers.
Aug 2026 - Release EP Agentic Authorization-Containment Benchmark v1 (open dataset, Apache-2.0/CC-BY) + run against GPT-5.5 / GPT-5.4 / GPT-5.3 Instant.
Sep 2026 - Verifier + OpenAI-Agents-adapter hardening; defender onboarding docs; convert the open external PRs into merged reference integrations.
Oct 2026 - Independent cryptographic implementation audit underway (canonicalization, signing, cross-verification).
Nov 2026 - Public benchmark results published; conformance suite v1 freeze.
Dec 2026 - Audit summary published; reference deployments documented.

### Requested funding / API credits
(unchanged) Up to $250,000 total, in $10,000 increments, API credits primary.
- ~$150,000 OpenAI API credits: the containment benchmark at scale (thousands of adversarial trials across GPT-5.5/5.4/5.3, repeated over the cycle) + codegen-assisted porting/test generation for the verifiers and the adapter.
- ~$60,000 direct: independent cryptographic implementation audit.
- ~$40,000 direct: PI engineering time + a part-time contract cryptographer.
Glad to right-size; a credits-only award funds the benchmark and verifier work and stands alone.

### Papers (author) link
https://datatracker.ietf.org/doc/draft-schrock-ep-authorization-receipts/ — IETF Internet-Draft (author: Iman Schrock); the specification this proposal implements and extends. (Now part of a posted 7-draft cluster under the same author.)

### PDF / Additional notes
PDF: left blank (proposal self-contained).
Additional notes: "Revision of our 2026-06-13 submission. Since then: posted the full IETF cluster, reached 58 cross-language conformance vectors, shipped an OpenAI Agents SDK receipt adapter, and opened the first external Receipt-Required integrations into third-party agent servers — plus this revision adds an openly-released benchmark dataset and model specifics. All EMILIA artifacts are open-source (Apache-2.0 / CC-BY); purely defensive; no confidential or personal data is included."
